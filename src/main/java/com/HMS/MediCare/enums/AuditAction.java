package com.HMS.MediCare.enums;

/**
 * Audit actions for tracking data access and modifications
 */
public enum AuditAction {
    CREATE,
    READ,
    UPDATE,
    DELETE,
    LOGIN,
    LOGOUT,
    ACCESS_DENIED,
    EXPORT,
    SEARCH
}
